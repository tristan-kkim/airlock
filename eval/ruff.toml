# Inherit the repository settings; the dataset generator holds long natural-language templates.
extend = "../pyproject.toml"

[lint.per-file-ignores]
"generate.py" = ["E501"]

[lint.isort]
known-first-party = [
  "attack", "calibrate_judges", "common", "compare", "generate", "gliner_pii", "mock_airlock",
  "presidio_ko", "protected", "protocol_estimate", "raw", "reframe", "regex_only", "reuse", "run",
  "run_agent_eval", "scoring", "subset", "utility",
]
