# Inherit the repository settings; the dataset generator holds long natural-language templates.
extend = "../pyproject.toml"

[lint.per-file-ignores]
"generate.py" = ["E501"]

[lint.isort]
known-first-party = [
  "attack", "common", "compare", "generate", "gliner_pii", "judge", "mock_airlock",
  "presidio_ko", "raw", "regex_only", "run", "scoring",
]
